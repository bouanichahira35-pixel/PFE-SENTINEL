## Frontend (Create React App)
## Base URL de l'API backend (inclure /api)
# Option 1 (recommandee): laisser vide => utilise le proxy CRA (package.json#proxy)
# REACT_APP_API_URL=
#
# Option 2: backend sur un autre host (ex: LAN) => inclure /api
REACT_APP_API_URL=http://localhost:5000/api

